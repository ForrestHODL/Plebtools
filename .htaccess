RewriteEngine On

# Redirect /treasury to treasury.html
RewriteRule ^treasury$ treasury.html [L]

# Optional: Redirect .html extension to clean URLs
RewriteCond %{THE_REQUEST} /([^.]+)\.html [NC]
RewriteRule ^ /%1? [NC,L,R=301]